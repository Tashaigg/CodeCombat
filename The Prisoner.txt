# Free the prisoner, defeat the guard and grab the gem.
hero.moveRight()
# FREEe Patrick from behind the "Weak Door".
hero.attack("Weak Door")
hero.moveRight(3)
# Defeat the guard, named "Two".
hero.attack("Two")
hero.attack("Two")
# Get the gem.
hero.moveDown(3)
